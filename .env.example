# Fortune Fusion Engine - Environment Variables

# Redis (optional - falls back to memory cache)
REDIS_URL=redis://localhost:6379/0

# PostgreSQL (optional - falls back to no persistence)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/fortune

# Logging
LOG_LEVEL=info

# Server
HOST=0.0.0.0
PORT=8000
